# Reimbursement Management - Environment Template
# Copy this file to .env.local and update values as needed.

# Required: MongoDB connection URI
MONGODB_URI=mongodb://127.0.0.1:27017/reimbursement-management

# Optional: Explicit DB name (falls back to URI database if omitted)
MONGODB_DB_NAME=reimbursement-management
